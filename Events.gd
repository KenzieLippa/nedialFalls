extends Node

#mainly for signals
#allows other parts of the game to talk to each other
signal add_screenshake(amount, duration)

signal door_entered(door)

signal info_box_entered(box)

signal info_box_exited(box)
